// TODO: Switch Jest to ESM or use babel-jest to import Next.js route modules.
// Placeholder tests to keep CI green while enabling future integration tests.

describe('Agency API (placeholder)', () => {
  test('GET /api/agency placeholder', () => {
    expect(true).toBe(true);
  });

  test('POST /api/agency/request placeholder', () => {
    expect(true).toBe(true);
  });
});
